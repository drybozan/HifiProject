@using HiFi.Common.Helpers
@model List<HiFi.Dto.SetupDto>

<button type="button" class=" viewforsetupadd btn btn-success w-20 mt-2" data-toggle="modal" data-target="/ProfileSettings/AddSetup/">yeni setup ekle</button>

@foreach (var item in Model)
{
    <div class="card-body ">
        <div class="input-group input-group-outline mb-3">
            @(Html.TextBoxForControl(m => @item.SetupName, new { placeholder = "Setup Adı", @readonly = true }))
        </div>
        <div class="input-group input-group-outline mb-3">
               @(Html.TextAreaForControl(m => @item.SetupDetail, new {  placeholder = "Setup Hakkında", rows = "5", @readonly = true }))
        </div>
        <div class="text-center">
            <div class="text-center">
                <a href="#" class="view btn bg-gradient-primary btn-lg w-20 mt-2 mb-0 mb-3" data-toggle="modal" data-target="/ProfileSettings/Singlesetup/@item.SetupId">Düzenle</a>
                <a href="#" class="viewaddpic btn bg-gradient-primary btn-lg w-20 mt-2 mb-0 mb-3" data-toggle="modal" data-target="/ProfileSettings/AddSetupPictures/@item.SetupId">Fotoğraf Ekle</a>
                <a href="#" class="viewforpic btn bg-gradient-primary btn-lg w-30 mt-2 mb-0 mb-3" data-toggle="modal" data-target="/ProfileSettings/SetupPictures/@item.SetupId">Fotoğrafları Düzenle</a>
            </div>
        </div>
    </div>
}

@*setup güncellme ve silme için modal*@

<div class="modal fade" id="modelView" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">SETUP</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">

            </div>

        </div>
    </div>
</div>
<script>
    $(function () {
        // a tagimizde bulunan .view classımıza click olduğunda
        $("body").on("click", ".view", function () {
            //data-target dan url mizi al
            var url = $(this).data("target");
            //bu urlimizi get et
            $.get(url, function (data) { })
                //eğer işlemimiz başarılı bir şekilde gerçekleşirse
                .done(function (data) {
                    //gelen datayı .modal-body mizin içerise html olarak ekle
                    $("#modelView .modal-body").html(data);
                    //sonra da modalimizi göster
                    $("#modelView").modal("show");
                })
                //eğer işlem başarısız olursa
                .fail(function () {
                    //modalımızın bodysine Error! yaz
                    $("#modelView .modal-body").text("Error!!");
                    //sonra da modalimizi göster
                    $("#modelView").modal("show");
                })
        });
    })
</script>

@*setup ekleme için modal*@
<div class="modal fade" id="modelViewforaddsetup" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">SETUP EKLE</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">

            </div>

        </div>
    </div>
</div>

<script>
    $(function () {
        // a tagimizde bulunan .viewforsetupadd classımıza click olduğunda
        $("body").on("click", ".viewforsetupadd", function () {
            //data-target dan url mizi al
            var url = $(this).data("target");
            //bu urlimizi get et
            $.get(url, function (data) { })
                //eğer işlemimiz başarılı bir şekilde gerçekleşirse
                .done(function (data) {
                    //gelen datayı .modal-body mizin içerise html olarak ekle
                    $("#modelViewforaddsetup .modal-body").html(data);
                    //sonra da modalimizi göster
                    $("#modelViewforaddsetup").modal("show");
                })
                //eğer işlem başarısız olursa
                .fail(function () {
                    //modalımızın bodysine Error! yaz
                    $("#modelViewforaddsetup .modal-body").text("Error!!");
                    //sonra da modalimizi göster
                    $("#modelViewforaddsetup").modal("show");
                })
        });
    })
</script>

<div class="modal fade" id="modelViewforpictures" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Fotoğraf Düzenle</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">

            </div>

        </div>
    </div>
</div>

<script>
    $(function () {
        // a tagimizde bulunan .viewforsetupadd classımıza click olduğunda
        $("body").on("click", ".viewforpic", function () {
            //data-target dan url mizi al
            var url = $(this).data("target");
            //bu urlimizi get et
            $.get(url, function (data) { })
                //eğer işlemimiz başarılı bir şekilde gerçekleşirse
                .done(function (data) {
                    //gelen datayı .modal-body mizin içerise html olarak ekle
                    $("#modelViewforpictures .modal-body").html(data);
                    //sonra da modalimizi göster
                    $("#modelViewforpictures").modal("show");
                })
                //eğer işlem başarısız olursa
                .fail(function () {
                    //modalımızın bodysine Error! yaz
                    $("#modelViewforpictures .modal-body").text("Error!!");
                    //sonra da modalimizi göster
                    $("#modelViewforpictures").modal("show");
                })
        });
    })
</script>
<div class="modal fade" id="modelViewforaddpic" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Fotoğraf Ekle</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body">

            </div>

        </div>
    </div>
</div>

<script>
    $(function () {
        // a tagimizde bulunan .viewforsetupadd classımıza click olduğunda
        $("body").on("click", ".viewaddpic", function () {
            //data-target dan url mizi al
            var url = $(this).data("target");
            //bu urlimizi get et
            $.get(url, function (data) { })
                //eğer işlemimiz başarılı bir şekilde gerçekleşirse
                .done(function (data) {
                    //gelen datayı .modal-body mizin içerise html olarak ekle
                    $("#modelViewforaddpic .modal-body").html(data);
                    //sonra da modalimizi göster
                    $("#modelViewforaddpic").modal("show");
                })
                //eğer işlem başarısız olursa
                .fail(function () {
                    //modalımızın bodysine Error! yaz
                    $("#modelViewforaddpic .modal-body").text("Error!!");
                    //sonra da modalimizi göster
                    $("#modelViewforaddpic").modal("show");
                })
        });
    })
</script>